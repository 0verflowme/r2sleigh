NAME=decompiler_bool_carrier_chain_full_snapshot
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaaa
s `afl~test_bool_carrier_chain[0]` >/dev/null
af
a:sla.dec | jq -Rs 'contains("test_bool_carrier_chain") and contains("if") and contains("return") and (contains("r2dec fallback:")|not)'
EOF_CMDS
RUN

NAME=decompiler_complex_check_stable_header_and_local_prefix
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~complex_check[0]` >/dev/null
af
a:sla.dec | jq -Rs 'contains("complex_check") and contains("diff = a - b;") and contains("return 0;") and contains("return 1;") and (contains("r2dec residual:")|not) and (contains("r2dec fallback:")|not)'
EOF_CMDS
RUN
