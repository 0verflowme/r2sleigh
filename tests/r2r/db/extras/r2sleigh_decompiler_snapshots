NAME=decompiler_bool_carrier_chain_full_snapshot
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
int64_t sym.test_bool_carrier_chain(intN_t arg1, intN_t arg2)
{
    if (arg1 != arg2) {
        return arg1;
    } else {
        return arg2;
    }
}
EOF_EXPECT
CMDS=<<EOF_CMDS
aaaa
a:sla.dec sym.test_bool_carrier_chain | python3 normalize_snapshot.py decompiler
EOF_CMDS
RUN

NAME=decompiler_complex_check_stable_header_and_local_prefix
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
int64_t sym.complex_check(intN_t arg1, intN_t arg2)
{
    if (arg1 + arg2 != 64) {
        var_4h = 0;
        var_4h = 0;
        return 0;
    } else {
        var_4h = 1;
        if (var_14h != 14) {
            return 1;
        }
        var_4h = 1;
        return 1;
    }
}
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
a:sla.dec sym.complex_check | python3 normalize_snapshot.py decompiler
EOF_CMDS
RUN
