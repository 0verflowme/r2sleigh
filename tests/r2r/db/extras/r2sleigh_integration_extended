NAME=public_help_hides_deprecated_debug_verbs
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
true
true
EOF_EXPECT
CMDS=<<EOF_CMDS
a:sla? | jq -Rs 'contains("a:sla.dec") and (contains("anal.sla")|not) and (contains("a:sla.debug.sym")|not)'
a:sym? | jq -Rs 'contains("a:sym.solve") and contains("a:sym.state") and (contains("a:sym.runj")|not)'
e??anal.sla | jq -Rs 'length==0'
EOF_CMDS
RUN

NAME=deprecated_debug_aliases_report_moved_namespace
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
true
true
true
true
EOF_EXPECT
CMDS=<<EOF_CMDS
a:sla.debug.mem >/dev/null
a:sla.mem | jq -Rs 'contains("moved to a:sla.debug.mem")'
a:sla.types | jq -Rs 'contains("moved to a:sla.debug.types")'
a:sla.profilej | jq -Rs 'contains("moved to a:sla.debug.profilej")'
a:sla.assumej [] | jq -Rs 'contains("moved to a:sla.debug.assumej")'
a:sym.runj {} | jq -Rs 'contains("moved to a:sym.debug.runj")'
EOF_CMDS
RUN

NAME=dec_check_secret_current_function_public_surface
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~check_secret[0]` >/dev/null
af
a:sla.dec | jq -Rs 'contains("check_secret") and contains("0xdead") and contains("return 1;") and contains("return 0;") and (contains("r2dec fallback:")|not)'
EOF_CMDS
RUN

NAME=pdd_routes_to_sleigh_decompiler_when_plugin_loaded
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~check_secret[0]` >/dev/null
af
pdd | jq -Rs 'contains("check_secret") and contains("return") and (contains("r2pm -ci r2dec")|not)'
EOF_CMDS
RUN

NAME=profilej_reports_deterministic_stage_timings
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
a:sla >/dev/null
aaaa
a:sla.debug.profilej | jq -c '.enabled==true and .max==20 and .count>0 and (.decompile_cache|has("hits") and has("misses")) and (.functions|type=="array") and (.functions|length>0) and (.functions[0]|has("addr") and has("total_us") and has("lift_us") and has("typed_context_us") and has("session_us") and has("mutation_us") and has("xref_us") and has("taint_us") and has("decompile_us"))'
EOF_CMDS
RUN

NAME=profilej_available_without_config
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
a:sla >/dev/null
aaa
a:sla.debug.profilej | jq -c '.enabled==true and .max==20 and .count>0'
EOF_CMDS
RUN

NAME=repeated_dec_reports_shared_render_cache_hit
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~check_secret[0]` >/dev/null
af
a:sla.dec >/dev/null
a:sla.dec >/dev/null
a:sla.debug.profilej | jq -c '.decompile_cache.hits >= 1 and .decompile_cache.misses >= 1'
EOF_CMDS
RUN

NAME=dec_check_secret_explicit_address_public_surface
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~check_secret[0]` >/dev/null
af
a:sla.dec $$ | jq -Rs 'contains("check_secret") and contains("0xdead") and contains("return 1;") and contains("return 0;") and (contains("r2dec fallback:")|not)'
EOF_CMDS
RUN

NAME=dec_solve_equation_header_not_void
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~solve_equation[0]` >/dev/null
af
a:sla.dec | head -n1 | jq -Rs 'contains("solve_equation") and (contains("int") or contains("uint")) and (contains("void ")|not)'
EOF_CMDS
RUN

NAME=dec_boolxor_preserves_xor_shape
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~test_boolxor[0]` >/dev/null
af
a:sla.dec | jq -Rs 'contains("test_boolxor") and contains("^") and (contains("r2dec fallback:")|not)'
EOF_CMDS
RUN

NAME=dec_array_and_struct_index_render_without_fallback
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaaa
s `afl~test_array_index[0]` >/dev/null
af
a:sla.dec | jq -Rs 'contains("test_array_index") and contains("return") and (contains("r2dec fallback:")|not)'
s `afl~test_struct_array_index[0]` >/dev/null
af
a:sla.dec | jq -Rs 'contains("test_struct_array_index") and contains("return") and (contains("r2dec fallback:")|not)'
EOF_CMDS
RUN

NAME=dec_library_call_owners_stay_single_source
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
true
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~process_string[0]` >/dev/null
af
a:sla.dec | jq -Rs 'contains("process_string") and contains("strlen") and contains("return") and (contains("r2dec fallback:")|not)'
s `afl~authenticate[0]` >/dev/null
af
a:sla.dec | jq -Rs 'contains("authenticate") and contains("strcmp") and contains("return") and (contains("r2dec fallback:")|not)'
s `afl~alloc_and_copy[0]` >/dev/null
af
a:sla.dec | jq -Rs 'contains("alloc_and_copy") and contains("malloc") and contains("memcpy") and contains("return") and (contains("r2dec fallback:")|not)'
EOF_CMDS
RUN

NAME=sym_solve_check_secret_public_target
FILE=malloc://64
ARGS=-a x86 -b 64 -e scr.color=false -e log.level=0
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
wx 4883ff007406b800000000c3b801000000c300000000000000
s 0 >/dev/null
af
a:sym.solve 0xc | jq -c '.found==true and (.selected_path.solution.inputs|length)>0'
EOF_CMDS
RUN

NAME=sym_solve_public_solution_filters_temp_and_flag_noise
FILE=malloc://64
ARGS=-a x86 -b 64 -e scr.color=false -e log.level=0
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
wx 4883ff007406b800000000c3b801000000c300000000000000
s 0 >/dev/null
af
a:sym.solve 0xc | jq -c '.found==true and (.selected_path.solution.inputs|keys|all((startswith("tmp:") or startswith("TMP") or .=="CY" or .=="NG" or .=="OV" or .=="ZR")|not))'
EOF_CMDS
RUN

NAME=sym_explore_check_secret_public_target
FILE=malloc://64
ARGS=-a x86 -b 64 -e scr.color=false -e log.level=0
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
wx 4883ff007406b800000000c3b801000000c300000000000000
s 0 >/dev/null
af
a:sym.explore 0xc | jq -c '.matched_paths>0 and (.paths|length)>0'
EOF_CMDS
RUN

NAME=sym_state_empty_before_run
FILE=malloc://64
ARGS=-a x86 -b 64 -e scr.color=false -e log.level=0
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
wx 4883ff007406b800000000c3b801000000c300000000000000
s 0 >/dev/null
af
a:sym.state | jq -c '.has_state==false'
EOF_CMDS
RUN

NAME=sym_state_after_solve_same_session
FILE=malloc://64
ARGS=-a x86 -b 64 -e scr.color=false -e log.level=0
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
wx 4883ff007406b800000000c3b801000000c300000000000000
s 0 >/dev/null
af
a:sym.solve 0xc >/dev/null
a:sym.state | jq -c '.has_state==true and .mode=="solve" and .result.found==true'
EOF_CMDS
RUN

NAME=sym_unreachable_target_refuses_without_claiming_proof
FILE=malloc://64
ARGS=-a x86 -b 64 -e scr.color=false -e log.level=0
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
wx 4883ff007406b800000000c3b801000000c300000000000000
s 0 >/dev/null
af
a:sym.solve 0xdeadbeef | jq -c '.found==false and (.witness.proven==false)'
EOF_CMDS
RUN

NAME=types_symbolic_xor_guard_reports_native_facts
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~test_symbolic_xor_guard[0]` >/dev/null
af
a:sla.debug.types | jq -c '.compiled_semantics.execution=="native" and (.compiled_semantics.region_count >= 1) and (.compiled_semantics.branches_pruned >= 0)'
a:sla.dec | jq -Rs 'contains("test_symbolic_xor_guard") and contains("return")'
EOF_CMDS
RUN

NAME=arm64_pointer_auth_lift_emits_semantic_userop
FILE=malloc://16
ARGS=-a arm -b 64 -e scr.color=false -e log.level=0
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
wx 7f2303d5000000000000000000000000
s 0 >/dev/null
a:sla.debug.json | jq -c '.[0].CallOther.userop == 4294901762'
EOF_CMDS
RUN

NAME=types_symbolic_helper_scope_exposes_interproc_payload
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~test_symbolic_helper_pure_guard[0]` >/dev/null
af
a:sla.debug.types | jq -c '.compiled_semantics.execution=="native" and (.interproc.scope.payloads // [] | length) >= 1'
EOF_CMDS
RUN

NAME=typed_assumptions_roundtrip_into_session_payload
FILE=bins/vuln_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~test_symbolic_helper_pure_guard[0]` >/dev/null
af
a:sla.debug.assumej [{"subject":{"register":{"name":"rdi"}},"value":{"type_hint":{"ty":"int32_t"}}}] >/dev/null
a:sla.debug.types | jq -c '(.assumptions.items // [] | any((.subject.register.name? // "" | ascii_downcase) == "rdi" and .value.type_hint.ty == "int32_t"))'
a:sla.debug.types | jq -c '(.params[0].type // "") == "int32_t"'
EOF_CMDS
RUN

NAME=type_session_mutation_plan_materializes_writeback_kinds
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~alloc_wrapper2[0]` >/dev/null
af
a:sla.debug.types | jq -c '([.mutation_plan.mutations[]?.kind] as $k | ($k | index("signature") != null) and ($k | index("callconv") != null) and (($k | index("var") != null) or ($k | index("var_type") != null)))'
EOF_CMDS
RUN

NAME=types_interproc_wrapper_uses_cached_callee_payload
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaaa
s `afl~alloc_wrapper2[0]` >/dev/null
af
a:sla.debug.types | jq -c '(.ret_type == "allocation_ptr") and (.interproc.summary != null) and (((.interproc.scope.payloads // []) | length) >= 1)'
EOF_CMDS
RUN

NAME=named_file_transfer_summary_feeds_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~main[0]` >/dev/null
afn copy_file_data
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("file_transfer")) != null) and (.params[1].type == "stat*") and (.params[8].type == "count_t")'
EOF_CMDS
RUN

NAME=named_sort_merge_summary_feeds_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~main[0]` >/dev/null
afn mergefps
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("record_stream")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("sort_merge")) != null) and (.params[0].type == "sortfile*") and (.params[3].type == "FILE*")'
EOF_CMDS
RUN

NAME=named_output_stream_summary_uses_canonical_helper_name
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~main[0]` >/dev/null
afn oputs_.constprop.0
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("output_stream")) != null)'
EOF_CMDS
RUN

NAME=named_hash_fold_summary_feeds_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn _md5_process_block
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("hash_fold")) != null) and (.params[1].type == "size_t") and (.params[2].type == "md5_ctx*")'
EOF_CMDS
RUN

NAME=named_numeric_transform_summary_feeds_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn gregorian_to_persian
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("numeric_transform")) != null) and (.ret_type == "int") and (.params[0].type == "calendar_date*") and (.params[0].name == "result")'
EOF_CMDS
RUN

NAME=named_randread_summary_feeds_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn randread
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("hash_fold")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("memory_write")) != null) and (.ret_type == "void") and (.params[0].type == "randread_source*") and (.params[1].type == "int8_t*") and (.params[2].type == "size_t")'
EOF_CMDS
RUN

NAME=named_pattern_parser_summary_feeds_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn _internal_fnwmatch
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("parser")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("string_scan")) != null) and ((.params[0].type // "") | contains("wchar_t")) and (.params[3].name == "no_leading_period")'
EOF_CMDS
RUN

NAME=named_getopt_parser_summary_feeds_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn _getopt_internal_r
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("parser")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("table_walk")) != null) and (.params[2].name == "optstring") and (.params[3].type == "option*") and (.params[6].type == "_getopt_data*")'
EOF_CMDS
RUN

NAME=named_digest_stream_summary_feeds_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn digest_file.isra.0
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("hash_fold")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("record_stream")) != null) and (.params[2].type == "size_t")'
EOF_CMDS
RUN

NAME=named_expression_eval_summary_feeds_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn binop
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("parser")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("table_walk")) != null) and (.ret_type == "int")'
EOF_CMDS
RUN

NAME=named_counter_output_summary_feeds_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn write_counts
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("format_render")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("output_stream")) != null) and (.ret_type == "void") and (.params[0].type == "uintmax_t") and (.params[4].type == "intmax_t")'
EOF_CMDS
RUN

NAME=named_diagnostic_summary_feeds_void_signature
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn verror_at_line
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("diagnostic_wrapper")) != null) and (.ret_type == "void") and (.params[3].type == "unsigned int") and ((.params[5].type // "") | contains("__va_list_tag"))'
EOF_CMDS
RUN

NAME=named_argmatch_summary_feeds_table_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn argmatch
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("string_scan")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("table_walk")) != null) and (.ret_type == "ptrdiff_t") and (.params[2].type == "argmatch_value*") and (.params[3].type == "size_t")'
EOF_CMDS
RUN

NAME=named_quotearg_summary_feeds_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn quotearg_n_options
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("string_scan")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("table_walk")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("format_render")) != null) and (.params[2].type == "size_t") and (.params[3].type == "quoting_options*")'
EOF_CMDS
RUN

NAME=named_selinux_path_summary_feeds_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn defaultcon
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("path_walk")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("metadata_probe")) != null) and (.ret_type == "int") and (.params[0].type == "selabel_handle*") and (.params[2].type == "mode_t")'
EOF_CMDS
RUN

NAME=named_renameatu_summary_feeds_path_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn renameatu
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("path_walk")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("metadata_probe")) != null) and (.ret_type == "int") and (.params[1].type == "int8_t*") and (.params[4].type == "unsigned int")'
EOF_CMDS
RUN

NAME=named_streamsavedir_summary_feeds_directory_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn streamsavedir
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("directory_traversal")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("allocation")) != null) and (.ret_type == "int8_t*") and (.params[0].type == "DIR*") and ((.params[1].type // "") | contains("savedir_option"))'
EOF_CMDS
RUN

NAME=named_quotearg_alloc_summary_feeds_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn quotearg_alloc_mem
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("allocation")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("string_scan")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("table_walk")) != null) and (.ret_type == "int8_t*") and (.params[2].type == "size_t*") and (.params[3].type == "quoting_options*")'
EOF_CMDS
RUN

NAME=named_xpalloc_summary_feeds_allocation_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn xpalloc
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("allocation")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("metadata_probe")) != null) and (.ret_type == "allocation_ptr") and (.params[0].type == "allocation_ptr") and (.params[1].type == "idx_t*") and (.params[3].type == "ptrdiff_t")'
EOF_CMDS
RUN

NAME=named_version_summary_feeds_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn version_etc_va
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("format_render")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("table_walk")) != null) and (.ret_type == "void") and (.params[0].type == "FILE*") and ((.params[4].type // "") | contains("__va_list_tag"))'
EOF_CMDS
RUN

NAME=named_multibyte_summary_feeds_types
FILE=bins/test_func_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `afl~add[0]` >/dev/null
afn rpl_mbrtowc
a:sla.debug.types | jq -c 'type=="object" and (.compiled_semantics.granularity == "summary_only") and (((.compiled_semantics.native_worker_summary_kinds // []) | index("parser")) != null) and (((.compiled_semantics.native_worker_summary_kinds // []) | index("string_scan")) != null) and (.params[0].type == "wchar_t*") and (.params[3].type == "mbstate_t*")'
EOF_CMDS
RUN

NAME=stress_parse_number_records_deterministic_output
FILE=bins/stress_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `isq~parse_number$[0]` >/dev/null
af
a:sla.debug.types | jq -c 'type=="object" and ((((.function_name // "") | contains("parse_number")) and ((.compiled_semantics // {}) | type == "object")) or (.error? | type == "string"))'
EOF_CMDS
RUN

NAME=stress_parse_number_decompile_uses_parser_summary_route
FILE=bins/stress_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `isq~parse_number$[0]` >/dev/null
af
a:sla.dec | jq -Rs 'contains("parse_number") and contains("parse base10 numeric") and contains("worker summary: parser") and (contains("str != 45")|not) and (contains("str != 43")|not) and (contains("r2dec fallback:")|not) and (contains("r2dec residual:")|not)'
EOF_CMDS
RUN

NAME=vm_shaped_large_cfg_records_scc_transfer_without_truncation
FILE=bins/stress_test_x86
ARGS=-e scr.color=false -e log.level=0 -e bin.relocs.apply=true
EXPECT=<<EOF_EXPECT
true
EOF_EXPECT
CMDS=<<EOF_CMDS
aaa
s `isq~tiny_vm_dispatch$[0]` >/dev/null
af
a:sla.debug.types | jq -c 'type=="object" and ((.function_name // "") | contains("tiny_vm_dispatch")) and .compiled_semantics.execution=="vm" and (.compiled_semantics.vm_transfer != null) and (((.compiled_semantics.vm_transfer.truncated_handlers // []) | length) == 0) and (((.compiled_semantics.vm_transfer.transfers // []) | length) > 0) and (((.compiled_semantics.vm_transfer.transfers // []) | all(.truncated == false))) and (((.compiled_semantics.vm_transfer.handler_state_updates // {}) | to_entries | length) >= 5)'
EOF_CMDS
RUN
